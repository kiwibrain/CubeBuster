server:
  port: ${PORT:8761}

spring:
  flyway:
    enabled: false
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
